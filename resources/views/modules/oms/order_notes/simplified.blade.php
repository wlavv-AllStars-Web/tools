{{-- Private, read-only OMS prototype. --}}
@extends('layouts.app')

@section('content')
<div class="container-fluid py-3 oms-simple">
<style>
.oms-simple{--line:#e4e7ec;--muted:#667085}.simple-card{background:#fff;border:1px solid var(--line);border-radius:8px;box-shadow:0 2px 8px #1018280a}.simple-head{padding:16px;border-bottom:1px solid var(--line)}.label{display:block;color:var(--muted);font-size:.7rem;font-weight:700;letter-spacing:.06em;text-transform:uppercase;margin-bottom:4px}.reference{font-size:1.15rem;font-weight:750}.meta,.conversion,.help,.name{color:var(--muted);font-size:.78rem}.table-wrap{overflow-x:auto}.simple-table{min-width:1180px;margin:0}.simple-table th{background:#f9fafb;color:#475467;font-size:.68rem;letter-spacing:.05em;text-transform:uppercase;white-space:nowrap}.simple-table th,.simple-table td{padding:.7rem .6rem;vertical-align:middle}.product{min-width:280px}.ref{display:block;font-weight:750}.name,.conversion{display:block;margin-top:3px}.quantity{display:flex;align-items:center;gap:6px;min-width:112px}.quantity input{width:65px;text-align:center}.simple-input[readonly]{background:#fff}.price{border-left:1px solid #f0f1f3;min-width:116px}.price input{min-width:96px;text-align:right}.state{width:28px;height:28px;display:inline-flex;justify-content:center;align-items:center;border-radius:5px}.complete{background:#dcfae6;border:1px solid #abefc6;color:#067647}.partial{background:#fef0c7;border:1px solid #fedf89;color:#b54708}.zero{background:#fee4e2;border:1px solid #fecdca;color:#b42318}.line-state .state{width:auto;gap:5px;padding:0 9px}.add-line td{background:#f8fbff;border-top:1px dashed #b2ccff}.empty{padding:2.5rem!important;text-align:center;color:var(--muted)}.help{padding:12px 16px}
</style>
<form class="simple-card" method="GET" action="{{ route('erp.oms.simple') }}">
 <div class="simple-head"><div class="row g-3 align-items-end">
  <div class="col-lg-4"><label class="label" for="supplier">Supplier</label><select id="supplier" class="form-select" name="supplier_id" onchange="this.form.submit()"><option value="">Select supplier…</option>@foreach($suppliers as $supplier)<option value="{{ $supplier->id_supplier }}" @selected((int)$selectedSupplierId === (int)$supplier->id_supplier)>{{ $supplier->name }}</option>@endforeach</select></div>
  <div class="col-lg-4"><label class="label" for="orderNote">Order note</label><select id="orderNote" class="form-select" name="order_note_id" @disabled($orderNotes->isEmpty()) onchange="this.form.submit()"><option value="">{{ $orderNotes->isEmpty() ? 'Select a supplier first…' : 'Select order note…' }}</option>@foreach($orderNotes as $candidate)<option value="{{ $candidate->id }}" @selected((int)optional($orderNote)->id === (int)$candidate->id)>{{ $candidate->reference }} · {{ str_replace('_',' ',$candidate->status) }}</option>@endforeach</select></div>
  <div class="col-lg-4">@if($orderNote)<span class="label">Current document</span><div class="reference">{{ $orderNote->reference }}</div><div class="meta">{{ $orderNote->supplier?->name }} · {{ str_replace('_',' ',$orderNote->status) }}</div>@else<div class="meta">Choose a supplier to load real OMS data.</div>@endif</div>
 </div></div>
 <div class="table-wrap"><table class="table simple-table align-middle"><thead><tr><th rowspan="2">Product</th><th colspan="3" class="text-center">Quantities</th><th colspan="2" class="text-center">Purchase price</th><th colspan="2" class="text-center">Sales price</th><th rowspan="2" class="text-center">Line state</th></tr><tr><th>Ordered</th><th>Invoiced</th><th>Received</th><th class="price">Supplier currency</th><th>EUR conversion</th><th class="price">Supplier currency</th><th>EUR conversion</th></tr></thead><tbody>
 @forelse($simplifiedOmsRows as $row)
 @php($ordered=(int)$row['ordered']) @php($invoiced=(int)$row['invoiced']) @php($received=(int)$row['received'])
 @php($invoiceState=$invoiced<=0?'zero':($invoiced>=$ordered?'complete':'partial')) @php($receivedState=$received<=0?'zero':($received===$invoiced?'complete':'partial')) @php($lineState=($invoiced<=0||$received<=0)?'zero':(($ordered===$invoiced&&$invoiced===$received)?'complete':'partial')) @php($icons=['complete'=>'fa-check','partial'=>'fa-minus','zero'=>'fa-xmark'])
 <tr><td class="product"><span class="ref">{{ $row['reference'] }}</span><span class="name">{{ $row['name'] }}</span></td><td><div class="quantity"><input class="form-control form-control-sm simple-input" readonly value="{{ $ordered }}"></div></td><td><div class="quantity"><span class="state {{ $invoiceState }}"><i class="fa-solid {{ $icons[$invoiceState] }}"></i></span><input class="form-control form-control-sm simple-input" readonly value="{{ $invoiced }}"></div></td><td><div class="quantity"><span class="state {{ $receivedState }}"><i class="fa-solid {{ $icons[$receivedState] }}"></i></span><input class="form-control form-control-sm simple-input" readonly value="{{ $received }}"></div></td><td class="price"><input class="form-control form-control-sm simple-input" readonly value="{{ number_format((float)$row['purchase_supplier'],6,'.','') }}"><small class="conversion">{{ $row['currency_iso'] }}</small></td><td><small class="conversion">€ {{ number_format((float)$row['purchase_eur'],6,',',' ') }}</small></td><td class="price"><input class="form-control form-control-sm simple-input" readonly value="{{ number_format((float)$row['sales_supplier'],6,'.','') }}"><small class="conversion">{{ $row['currency_iso'] }}</small></td><td><small class="conversion">€ {{ number_format((float)$row['sales_eur'],6,',',' ') }}</small></td><td class="line-state text-center"><span class="state {{ $lineState }}"><i class="fa-solid {{ $icons[$lineState] }}"></i><span>{{ ucfirst($lineState) }}</span></span></td></tr>
 @empty<tr><td colspan="9" class="empty">{{ $selectedSupplierId ? 'No order-note lines found.' : 'Select a supplier to display real OMS data.' }}</td></tr>@endforelse
 @if($orderNote)<tr class="add-line"><td class="product"><input class="form-control form-control-sm" placeholder="Add product by reference or name…" disabled><span class="name">Permanent final row; adding is disabled in this read-only prototype.</span></td><td colspan="8" class="small text-muted">This row will become the product search/add flow after approval.</td></tr>@endif
 </tbody></table></div><div class="help">Invoice: red at 0, amber while partial, green when equal to ordered. Received: red at 0, amber while partial, green when equal to invoiced. Line: red if invoiced or received is 0; otherwise amber until all quantities match.</div>
</form></div>
@endsection
